data:
  root: data/CZCE/sa
  min1_subdir: min1
  tick_subdir: ticks

strategy:
  name: dual_thrust
  lookback_days: 5
  k1: 0.50
  k2: 0.50
  max_hold_bars: 60

risk:
  max_position: 1
  max_daily_loss: 5000.0
  force_flat_time: "14:55:00"
  max_consecutive_losses: 3
  cooldown_minutes: 20

execution:
  contract_multiplier: 20.0
  tick_size: 1.0
  fee_rate: 0.00005
  slippage_ticks: 1.0
  impact_coeff: 0.2

database:
  enabled: true
  path: artifacts/sa_intraday.sqlite

output:
  artifacts_dir: artifacts
  initial_capital: 1000000.0

logging:
  level: INFO
  console: true
  file: true
  log_dir: artifacts/logs
  backtest_heartbeat_bars: 1200
